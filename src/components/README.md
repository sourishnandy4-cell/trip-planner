# Components Directory

**Owner**: Person A (Frontend Developer)

## Components to Build

- `Sidebar.jsx` — Navigation sidebar
- `Header.jsx` — Top header with trip info
- `ItineraryTimeline.jsx` — Vertical timeline of activities
- `BudgetPieChart.jsx` — Expense breakdown by category
- `RecentExpenses.jsx` — List of recent expenses
- `BalanceSheet.jsx` — Who owes whom table

## Data Source

Use `src/mockData/index.js` for initial development. Person C will provide service functions to swap in live data later.

## Column Names

Refer to `DATABASE_SCHEMA.md` for exact column names and data types.
