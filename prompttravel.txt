# 🗺️ Group Travel Planner — Distributed Build Prompts
### A 3-Person Collaborative GitHub Project

---

## 📁 Recommended Repo Structure

```
group-travel-planner/
├── README.md
├── .env.example
├── package.json
│
├── src/
│   ├── components/          ← Person A owns this
│   │   ├── Sidebar.jsx
│   │   ├── Header.jsx
│   │   ├── ItineraryTimeline.jsx
│   │   ├── BudgetPieChart.jsx
│   │   ├── RecentExpenses.jsx
│   │   └── BalanceSheet.jsx
│   │
│   ├── lib/                 ← Person C owns this
│   │   ├── supabaseClient.js
│   │   ├── itineraryService.js
│   │   ├── expenseService.js
│   │   └── balanceCalculator.js
│   │
│   ├── mockData/            ← Shared by all, seeded from schema
│   │   └── index.js
│   │
│   └── App.jsx              ← Person A assembles
│
└── database/                ← Person B owns this
    ├── schema.sql
    ├── rls_policies.sql
    └── seed.sql
```

---

## 🌿 GitHub Branching Strategy

```
main
└── dev
    ├── feature/frontend-dashboard     ← Person A
    ├── feature/database-schema        ← Person B
    └── feature/api-services           ← Person C
```

- All PRs target `dev`, never `main` directly.
- Tag Person B when your branch needs DB columns confirmed.
- Tag Person C when your component is ready to swap mock data for live calls.
- Person A merges to `dev` only after Person C's services are integrated.

---

---

# 👤 PERSON A — Frontend Developer

## Your Role
You own the entire UI layer: layout, components, styling, and interactivity.
Work against `mockData/index.js` first. Person C will swap those imports for live
Supabase calls once the schema (Person B) and services (Person C) are ready.

## Branch
`feature/frontend-dashboard`

## Deliverables
- [ ] `src/App.jsx` — root layout, state, and routing shell
- [ ] `src/components/Sidebar.jsx`
- [ ] `src/components/Header.jsx`
- [ ] `src/components/ItineraryTimeline.jsx`
- [ ] `src/components/BudgetPieChart.jsx`
- [ ] `src/components/RecentExpenses.jsx`
- [ ] `src/components/BalanceSheet.jsx`
- [ ] `src/mockData/index.js` — all mock state with JSDoc shape comments

---

## Your Prompt

> Act as an expert frontend developer. Build a responsive, modern **Group Travel
> Planner Dashboard** using **React** and **Tailwind CSS**. Follow the full spec below.

### Design System
- **Background**: `#F9F8F4` (warm off-white)
- **Primary accent**: `#2E3F5C` (deep slate blue)
- **Secondary accent**: `#E8A87C` (warm amber — used for CTAs and chart highlights)
- **Card surface**: `#FFFFFF` with `shadow-sm` and `rounded-2xl`
- **Font**: Use `font-sans` (set `Geist` or `DM Sans` via Google Fonts in `index.html`)
- All interactive elements must have `hover:` Tailwind states and `transition-all duration-200`

---

### Layout Architecture

Build a **3-zone layout**: Sidebar | Main Content | (responsive collapse on mobile).

```
┌──────────┬─────────────────────────────────────────────┐
│          │  Header Bar                                  │
│ Sidebar  ├───────────────────┬─────────────────────────┤
│ (fixed)  │  Itinerary        │  Budget Pie Chart        │
│          │  Timeline         │  Recent Expenses         │
│          │                   │  Balance Sheet           │
└──────────┴───────────────────┴─────────────────────────┘
```

On mobile (`< md`): sidebar collapses to a bottom nav bar; columns stack vertically.

---

### Component Specs

#### `Sidebar.jsx`
- Fixed left, `w-64`, background `#2E3F5C`, text white
- Logo at top: a small plane icon + "Wandr" in bold
- Nav items (with Heroicons or Lucide icons): Dashboard (active state), Itinerary, Expenses, Docs
- Active item: `bg-white/10 rounded-xl` highlight
- Bottom: user avatar + name + logout icon

#### `Header.jsx`
- Props: `{ tripName, dateRange, user }`
- Left: Trip title ("Barcelona Group Trip") + date badge ("July 12–19")
- Center: Search bar (`rounded-full`, soft border)
- Right: Bell icon (with red dot badge), user avatar + dropdown ("Sarah J." / "Settings" / "Sign Out")

#### `ItineraryTimeline.jsx`
- Props: `{ items: ItineraryItem[] }` — see mock data shape below
- Vertical timeline: left-side date labels (Mon 12, Tue 13, Wed 14), right-side activity cards
- The vertical line is `border-l-2 border-slate-200`
- Each **Activity Card** (`rounded-xl`, `shadow-sm`) shows:
  - Time (e.g., `10:00 AM`)
  - Activity title (bold)
  - Location / notes (muted text)
  - Category icon (use Lucide: `Bus`, `Utensils`, `MapPin`, `Music`)
  - Hover: card lifts with `hover:shadow-md hover:-translate-y-0.5`

#### `BudgetPieChart.jsx`
- Props: `{ totalBudget, categories: ExpenseCategory[] }`
- Use **Recharts** `PieChart` with `innerRadius` (donut style)
- Center label: `€4,500.00` in large bold, "Total Budget" below in muted text
- Legend below chart: colored dot + category name + amount
- Color map: Accommodation `#2E3F5C`, Food `#E8A87C`, Activities `#6DB8A8`, Transport `#C4B5D0`

#### `RecentExpenses.jsx`
- Props: `{ expenses: Expense[] }`
- Scrollable list (max height `300px`, `overflow-y-auto`)
- Each row: description | amount (right-aligned, bold) | "Paid by X" (muted badge)
- Alternating subtle row backgrounds: `even:bg-slate-50`

#### `BalanceSheet.jsx`
- Props: `{ balances: Balance[] }`
- Each row: "[Name] owes [Name] €XX.00" with colored amount (red for owed, green for receiving)
- Bottom: a prominent `SETTLE UP` button — `bg-[#E8A87C] text-white font-bold rounded-xl px-6 py-3 w-full`

---

### Mock Data Shape (`src/mockData/index.js`)
Create this file with clearly typed JSDoc comments so Person C can drop in live
service calls by simply changing the import source.

```js
// mockData/index.js
// HANDOFF NOTE for Person C:
// Replace these exports with calls from src/lib/itineraryService.js
// and src/lib/expenseService.js once Supabase is connected.

export const TRIP_META = {
  id: 'trip-001',
  name: 'Barcelona Group Trip',
  destination: 'Barcelona, Spain',
  start_date: '2025-07-12',
  end_date: '2025-07-19',
};

/** @type {Array<{id:string, start_time:string, title:string, location:string, notes:string, category_icon:string}>} */
export const ITINERARY_ITEMS = [
  { id: 'i1', start_time: '2025-07-12T10:00:00', title: 'Sagrada Familia Tour',
    location: 'Carrer de Mallorca', notes: 'Booked tickets in advance', category_icon: 'activity' },
  { id: 'i2', start_time: '2025-07-12T14:00:00', title: 'Tapas Lunch at El Xampanyet',
    location: 'El Born', notes: '', category_icon: 'food' },
  { id: 'i3', start_time: '2025-07-13T09:30:00', title: 'Airport Bus to City Center',
    location: 'T1 Terminal', notes: 'Aerobus — Line A1', category_icon: 'transport' },
  { id: 'i4', start_time: '2025-07-14T21:00:00', title: 'Jazz Club Jamboree',
    location: 'Plaça Reial 17', notes: 'Dress code: smart casual', category_icon: 'music' },
];

/** @type {Array<{id:string, category:string, amount:number}>} */
export const EXPENSE_CATEGORIES = [
  { id: 'cat1', category: 'Accommodation', amount: 1800 },
  { id: 'cat2', category: 'Food & Drinks',  amount: 950  },
  { id: 'cat3', category: 'Activities',     amount: 600  },
  { id: 'cat4', category: 'Transport',      amount: 350  },
];

export const TOTAL_BUDGET = 4500;

/** @type {Array<{id:string, description:string, amount:number, paid_by:string}>} */
export const RECENT_EXPENSES = [
  { id: 'e1', description: 'Sagrada Familia Tickets', amount: 110, paid_by: 'Sarah' },
  { id: 'e2', description: 'Hotel Vila Olímpica (3 nights)', amount: 630, paid_by: 'Mike' },
  { id: 'e3', description: 'Aerobus Passes x3', amount: 45,  paid_by: 'Chloe' },
  { id: 'e4', description: 'Dinner at Tickets Restaurant', amount: 210, paid_by: 'Sarah' },
];

/** @type {Array<{from:string, to:string, amount:number}>} */
export const BALANCES = [
  { from: 'Mike',  to: 'Sarah', amount: 84 },
  { from: 'Chloe', to: 'You',   amount: 22 },
];
```

---

### Coordination Notes for Person A
- **Do not** import from `src/lib/` yet — use `src/mockData/index.js` only.
- Export every component as a **named export** so Person C can wrap them cleanly.
- Leave a `// TODO: swap mock data` comment at every place you import from mockData.
- Open a PR to `dev` tagged **"Ready for API integration"** when components are stable.

---
---

# 👤 PERSON B — Database Architect

## Your Role
You own the entire data layer: PostgreSQL schema, indexes, RLS policies, and a
seed file. You are the source of truth for all column names and types — both
Person A (mock data shapes) and Person C (service functions) depend on your schema.

## Branch
`feature/database-schema`

## Deliverables
- [ ] `database/schema.sql` — full schema with foreign keys and indexes
- [ ] `database/rls_policies.sql` — Row Level Security rules
- [ ] `database/seed.sql` — Barcelona trip sample data matching Person A's mock data
- [ ] Update `README.md` with setup instructions and `.env.example`

---

## Your Prompt

> Act as a senior Database Architect. We are building a collaborative group travel
> planner and expense-splitting web app using **PostgreSQL via Supabase**. Design
> the complete relational schema and security layer described below.

### Requirements
Your schema must support three frontend views:
1. **Itinerary Timeline** — ordered list of activities per trip per day
2. **Budget Pie Chart** — category-level expense aggregations
3. **Balance Sheet** — net peer-to-peer debt calculation ("who owes whom")

---

### Schema: `database/schema.sql`

Implement and annotate each table below. Add any missing columns you think are
necessary for production-readiness (e.g., `updated_at`, `is_deleted` soft deletes).

```sql
-- Enable UUID generation
CREATE EXTENSION IF NOT EXISTS "pgcrypto";

-- 1. TRIPS
CREATE TABLE trips (
    id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name         VARCHAR(255) NOT NULL,
    destination  VARCHAR(255) NOT NULL,
    start_date   DATE NOT NULL,
    end_date     DATE NOT NULL,
    total_budget DECIMAL(10,2) DEFAULT 0,
    created_by   UUID REFERENCES auth.users(id),
    created_at   TIMESTAMPTZ DEFAULT now(),
    updated_at   TIMESTAMPTZ DEFAULT now()
);

-- 2. TRIP MEMBERS (many-to-many: users ↔ trips)
CREATE TABLE trip_members (
    id         UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    trip_id    UUID REFERENCES trips(id) ON DELETE CASCADE,
    user_id    UUID REFERENCES auth.users(id) ON DELETE CASCADE,
    role       VARCHAR(50) DEFAULT 'member',  -- 'owner' | 'member'
    joined_at  TIMESTAMPTZ DEFAULT now(),
    UNIQUE(trip_id, user_id)
);

-- 3. ITINERARY ITEMS (powers the vertical timeline)
CREATE TABLE itinerary_items (
    id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    trip_id       UUID REFERENCES trips(id) ON DELETE CASCADE,
    title         VARCHAR(255) NOT NULL,
    location      VARCHAR(255),
    start_time    TIMESTAMPTZ NOT NULL,
    end_time      TIMESTAMPTZ,
    notes         TEXT,
    category_icon VARCHAR(50) DEFAULT 'activity',
    -- Allowed values: 'activity' | 'food' | 'transport' | 'music' | 'accommodation'
    created_by    UUID REFERENCES auth.users(id),
    created_at    TIMESTAMPTZ DEFAULT now()
);

-- 4. EXPENSES (individual bills, tied to a trip and a payer)
CREATE TABLE expenses (
    id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    trip_id      UUID REFERENCES trips(id) ON DELETE CASCADE,
    description  VARCHAR(255) NOT NULL,
    amount       DECIMAL(10,2) NOT NULL CHECK (amount > 0),
    category     VARCHAR(50) NOT NULL,
    -- Allowed values: 'Accommodation' | 'Food & Drinks' | 'Activities' | 'Transport'
    paid_by      UUID REFERENCES auth.users(id),
    created_at   TIMESTAMPTZ DEFAULT now()
);

-- 5. EXPENSE SPLITS (who owes what portion of each expense)
CREATE TABLE expense_splits (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    expense_id  UUID REFERENCES expenses(id) ON DELETE CASCADE,
    user_id     UUID REFERENCES auth.users(id) ON DELETE CASCADE,
    owed_amount DECIMAL(10,2) NOT NULL CHECK (owed_amount > 0),
    is_settled  BOOLEAN DEFAULT FALSE,
    settled_at  TIMESTAMPTZ,
    UNIQUE(expense_id, user_id)
);
```

#### Required Indexes
```sql
-- Fast timeline queries
CREATE INDEX idx_itinerary_trip_time
    ON itinerary_items(trip_id, start_time ASC);

-- Fast expense aggregations for the pie chart
CREATE INDEX idx_expenses_trip_category
    ON expenses(trip_id, category);

-- Fast balance calculation queries
CREATE INDEX idx_splits_user_settled
    ON expense_splits(user_id, is_settled);

CREATE INDEX idx_splits_expense
    ON expense_splits(expense_id);

-- Fast membership lookups
CREATE INDEX idx_trip_members_user
    ON trip_members(user_id);
```

---

### RLS Policies: `database/rls_policies.sql`

```sql
-- Enable RLS on all tables
ALTER TABLE trips           ENABLE ROW LEVEL SECURITY;
ALTER TABLE trip_members    ENABLE ROW LEVEL SECURITY;
ALTER TABLE itinerary_items ENABLE ROW LEVEL SECURITY;
ALTER TABLE expenses        ENABLE ROW LEVEL SECURITY;
ALTER TABLE expense_splits  ENABLE ROW LEVEL SECURITY;

-- Helper function: is the current user a member of this trip?
CREATE OR REPLACE FUNCTION is_trip_member(trip_uuid UUID)
RETURNS BOOLEAN AS $$
  SELECT EXISTS (
    SELECT 1 FROM trip_members
    WHERE trip_id = trip_uuid
      AND user_id = auth.uid()
  );
$$ LANGUAGE sql SECURITY DEFINER;

-- TRIPS: members can read; only the creator can update/delete
CREATE POLICY "Members can view their trips"
    ON trips FOR SELECT
    USING (is_trip_member(id));

CREATE POLICY "Creator can manage trip"
    ON trips FOR ALL
    USING (created_by = auth.uid());

-- ITINERARY: all members can read and insert; only creator can delete
CREATE POLICY "Members can view itinerary"
    ON itinerary_items FOR SELECT
    USING (is_trip_member(trip_id));

CREATE POLICY "Members can add itinerary items"
    ON itinerary_items FOR INSERT
    WITH CHECK (is_trip_member(trip_id));

CREATE POLICY "Creator can delete itinerary items"
    ON itinerary_items FOR DELETE
    USING (created_by = auth.uid());

-- EXPENSES & SPLITS: all members can read; payer can insert
CREATE POLICY "Members can view expenses"
    ON expenses FOR SELECT
    USING (is_trip_member(trip_id));

CREATE POLICY "Members can add expenses"
    ON expenses FOR INSERT
    WITH CHECK (is_trip_member(trip_id));

CREATE POLICY "Members can view splits"
    ON expense_splits FOR SELECT
    USING (
        EXISTS (
            SELECT 1 FROM expenses e
            WHERE e.id = expense_splits.expense_id
              AND is_trip_member(e.trip_id)
        )
    );
```

---

### Seed File: `database/seed.sql`

Write a seed file that inserts the **exact same Barcelona trip data** as Person A's
`mockData/index.js` (copy their item titles, amounts, and categories verbatim) so
that when Person C switches from mock to live data, the UI looks identical.

---

### Coordination Notes for Person B
- Publish your final column names in a `DATABASE_SCHEMA.md` at the repo root
  so Persons A and C can reference it without reading raw SQL.
- If you rename a column after Person C has started, open a PR comment tagging them.
- The `category_icon` allowed values must **exactly match** the strings Person A
  uses in `mockData/index.js` (`'activity'`, `'food'`, `'transport'`, `'music'`).

---
---

# 👤 PERSON C — Backend Integration Engineer

## Your Role
You own the bridge between the database (Person B) and the UI (Person A).
You write all Supabase service functions, the balance calculator, and the
data-shape adapters that map DB rows to the prop types Person A expects.

## Branch
`feature/api-services`

## Deliverables
- [ ] `src/lib/supabaseClient.js` — initialized client (reads from `.env`)
- [ ] `src/lib/itineraryService.js` — fetch and format itinerary items
- [ ] `src/lib/expenseService.js` — fetch expenses, compute category totals
- [ ] `src/lib/balanceCalculator.js` — net debt aggregation function
- [ ] Update every `// TODO: swap mock data` comment in Person A's components

---

## Your Prompt

> Act as a senior backend integration engineer. We are building a collaborative
> group travel planner. Using **Supabase JS v2**, write the complete service layer
> that fetches data from our PostgreSQL schema and transforms it into the exact
> shapes the React frontend components expect.

### `src/lib/supabaseClient.js`
```js
import { createClient } from '@supabase/supabase-js';

const SUPABASE_URL = import.meta.env.VITE_SUPABASE_URL;
const SUPABASE_ANON_KEY = import.meta.env.VITE_SUPABASE_ANON_KEY;

if (!SUPABASE_URL || !SUPABASE_ANON_KEY) {
  throw new Error('Missing Supabase environment variables. Copy .env.example → .env');
}

export const supabase = createClient(SUPABASE_URL, SUPABASE_ANON_KEY);
```

---

### `src/lib/itineraryService.js`

Write a function `fetchItinerary(tripId)` that:
1. Queries `itinerary_items` filtered by `trip_id`, ordered by `start_time ASC`
2. Maps each row to the shape Person A's `ItineraryTimeline` component expects:
   ```js
   {
     id: string,
     start_time: string,   // ISO string — component formats locally
     title: string,
     location: string,
     notes: string,
     category_icon: string // 'activity' | 'food' | 'transport' | 'music'
   }
   ```
3. Returns `{ data, error }` — never throws, always returns structured result

```js
import { supabase } from './supabaseClient';

/**
 * Fetch all itinerary items for a trip, ordered chronologically.
 * @param {string} tripId - UUID of the trip
 * @returns {Promise<{data: ItineraryItem[]|null, error: Error|null}>}
 */
export const fetchItinerary = async (tripId) => {
  const { data, error } = await supabase
    .from('itinerary_items')
    .select('id, title, location, start_time, notes, category_icon')
    .eq('trip_id', tripId)
    .order('start_time', { ascending: true });

  if (error) {
    console.error('[fetchItinerary]', error.message);
    return { data: null, error };
  }
  return { data, error: null };
};
```

---

### `src/lib/expenseService.js`

Write two functions:

**`fetchRecentExpenses(tripId)`** — returns the last 10 expenses with payer display name:
```js
// Expected shape for RecentExpenses component:
// { id, description, amount, paid_by: string (display name), category }
```

**`fetchCategoryTotals(tripId)`** — aggregates expenses by category for the pie chart:
```js
/**
 * Aggregate expense amounts per category.
 * Returns array matching BudgetPieChart's `categories` prop.
 *
 * Strategy: fetch all expenses for the trip, then reduce by category in JS.
 * (Alternatively, use a Postgres RPC for large datasets.)
 */
export const fetchCategoryTotals = async (tripId) => {
  const { data, error } = await supabase
    .from('expenses')
    .select('category, amount')
    .eq('trip_id', tripId);

  if (error) return { data: null, error };

  const totals = data.reduce((acc, row) => {
    acc[row.category] = (acc[row.category] || 0) + Number(row.amount);
    return acc;
  }, {});

  const formatted = Object.entries(totals).map(([category, amount]) => ({
    category,
    amount,
  }));

  return { data: formatted, error: null };
};
```

---

### `src/lib/balanceCalculator.js`

Write `calculateNetBalances(tripId)`. This is the most complex function.

**Algorithm:**
1. Fetch all `expenses` (amount + paid_by) for the trip
2. Fetch all `expense_splits` (user_id + owed_amount + is_settled) joined to those expenses
3. For each unsettled split where `user_id ≠ paid_by`:
   - The `user_id` owes the `paid_by` user `owed_amount`
4. Collapse to **net balances**: if A owes B €50 and B owes A €30, net is A owes B €20
5. Return array matching `BalanceSheet`'s `balances` prop: `{ from, to, amount }`

```js
/**
 * Compute net peer-to-peer balances for a trip.
 * @param {string} tripId
 * @returns {Promise<{data: Balance[]|null, error: Error|null}>}
 *
 * @typedef {{ from: string, to: string, amount: number }} Balance
 */
export const calculateNetBalances = async (tripId) => {
  // Step 1: fetch expenses with payer info
  const { data: expenses, error: expErr } = await supabase
    .from('expenses')
    .select('id, paid_by, amount')
    .eq('trip_id', tripId);

  if (expErr) return { data: null, error: expErr };

  const expenseMap = Object.fromEntries(expenses.map(e => [e.id, e.paid_by]));

  // Step 2: fetch unsettled splits
  const expenseIds = expenses.map(e => e.id);
  const { data: splits, error: splitErr } = await supabase
    .from('expense_splits')
    .select('expense_id, user_id, owed_amount')
    .in('expense_id', expenseIds)
    .eq('is_settled', false);

  if (splitErr) return { data: null, error: splitErr };

  // Step 3 & 4: net balance map — key: "fromId→toId"
  const netMap = {};

  for (const split of splits) {
    const payer = expenseMap[split.expense_id];
    if (!payer || payer === split.user_id) continue;

    const fwd = `${split.user_id}→${payer}`;
    const rev = `${payer}→${split.user_id}`;

    if (netMap[rev] !== undefined) {
      netMap[rev] -= Number(split.owed_amount);
      if (netMap[rev] < 0) {
        netMap[fwd] = -netMap[rev];
        delete netMap[rev];
      } else if (netMap[rev] === 0) {
        delete netMap[rev];
      }
    } else {
      netMap[fwd] = (netMap[fwd] || 0) + Number(split.owed_amount);
    }
  }

  // Step 5: format for UI — replace UUIDs with display names
  // NOTE: extend this to join auth.users or a profiles table for real names
  const balances = Object.entries(netMap).map(([key, amount]) => {
    const [from, to] = key.split('→');
    return { from, to, amount: parseFloat(amount.toFixed(2)) };
  });

  return { data: balances, error: null };
};
```

---

### Integration Checklist for Person C

After Person A marks their PR "Ready for API integration":

1. In each of Person A's components, replace:
   ```js
   import { ITINERARY_ITEMS } from '../mockData';  // before
   import { fetchItinerary } from '../lib/itineraryService'; // after
   ```
2. Wrap data fetching in `useEffect` + `useState` (or React Query if the team agrees).
3. Add loading skeletons — at minimum a `<div className="animate-pulse ...">` placeholder.
4. Add error states — at minimum a red banner: `"Failed to load data. Try again."`
5. Test with the seed data from Person B's `seed.sql` to confirm the UI looks identical
   to mock data before merging.

---
---

## 🔗 Shared Contracts (All 3 Must Agree Before Writing Code)

Before anyone writes a line of code, create a `CONTRACTS.md` at the repo root and
lock these decisions together:

| Contract | Value | Agreed By |
|---|---|---|
| `category_icon` allowed strings | `activity`, `food`, `transport`, `music`, `accommodation` | A + B |
| Expense categories (exact strings) | `Accommodation`, `Food & Drinks`, `Activities`, `Transport` | A + B + C |
| User display name source | `profiles` table or `auth.users.email` | B + C |
| Date/time format passed to components | ISO 8601 string, formatted in component | A + C |
| Error handling pattern | `{ data, error }` tuple (no throws) | A + C |
| Framework | React + Vite + Tailwind CSS v3 | A |
| Supabase JS version | `@supabase/supabase-js@2` | B + C |

---

## ✅ Integration Milestones

```
Week 1
  Day 1–2:  All 3 agree on CONTRACTS.md → merge to dev
  Day 2–5:  Person B completes schema + seed (PR to dev)
             Person A builds all components against mock data (PR to dev)
             Person C writes service layer against schema (PR to dev)

Week 2
  Day 1–2:  Person C integrates services into Person A's components
  Day 3:    End-to-end test against Supabase staging with seed data
  Day 4:    Fix integration bugs, polish loading/error states
  Day 5:    Merge dev → main, deploy preview
```
